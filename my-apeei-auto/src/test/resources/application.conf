bddcore {

  request {

    server {
      host = "Host URL"
      authTokenHost= "Authtoken host"
      csSCAPayments = ""
    }

    defaults {

      headers = [
        {"Content-Type": "application/json"},
        {"x-lbg-channel": "IB"},
        {"ENVIRONMENT": "en"}
      ]

    }

  }
flags{
  randomCorrelation:false
  }
  files {

    restLogDir = "logs/rest-assured"
    testdata = "testdata/sit09/{testdata_name}.yml"
    schema = "src/test/resources/schema/schema_name.json"

  }
  http {
    connection {
      requestTimeout = 90000
      socketTimeout = 100000
      managerTimeout = 90000
    }
  }
}